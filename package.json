{
  "name": "claude-sync",
  "version": "1.0.0",
  "description": "Sync skills, plugins, and extensions between Claude Code and Claude Desktop",
  "type": "module",
  "bin": {
    "claude-sync": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run src/index.ts",
    "prepublishOnly": "bun run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "claude-desktop",
    "sync",
    "mcp",
    "extensions"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "picocolors": "^1.0.0",
    "diff": "^5.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/diff": "^5.2.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
